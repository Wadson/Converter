<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ConverPro.Pages.CompressPage" xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:models="clr-namespace:ConverPro.Models" xmlns:vm="clr-namespace:ConverPro.ViewModels" x:DataType="vm:MainViewModel" Title="Comprimir MP3">
 <Grid Padding="30,24" RowDefinitions="*,Auto" RowSpacing="16"><ScrollView><VerticalStackLayout Spacing="14"><Label Text="Comprimir MP3" Style="{StaticResource PageTitle}"/><Label Text="Reduza o tamanho dos arquivos mantendo os originais intactos." Style="{StaticResource PageSubtitle}"/><Border Style="{StaticResource Card}"><VerticalStackLayout Spacing="14">
  <HorizontalStackLayout Spacing="8"><Button Text="Selecionar arquivos MP3" ImageSource="action_add.png" Style="{StaticResource SecondaryButton}" Clicked="Pick"/><Button Text="Remover selecionados" ImageSource="action_remove.png" Style="{StaticResource DangerButton}" Clicked="Remove"/></HorizontalStackLayout>
  <CollectionView ItemsSource="{Binding Compressions}" HeightRequest="320"><CollectionView.EmptyView><Label Text="Nenhum MP3 selecionado." Style="{StaticResource EmptyLabel}"/></CollectionView.EmptyView><CollectionView.ItemTemplate><DataTemplate x:DataType="models:MediaQueueItem"><Border Stroke="#E5EAF1" StrokeShape="RoundRectangle 8" Padding="8" Margin="0,2"><Grid ColumnDefinitions="Auto,*,110" RowDefinitions="Auto,Auto"><CheckBox IsChecked="{Binding IsSelected}"/><VerticalStackLayout Grid.Column="1"><Label Text="{Binding Title}"/><Label Text="{Binding SizeSummary}" FontSize="11" TextColor="{StaticResource TextSecondary}"/><Label Text="{Binding Error}" TextColor="{StaticResource Danger}" FontSize="11"/></VerticalStackLayout><Label Grid.Column="2" Text="{Binding StatusText}"/><ProgressBar Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Progress="{Binding Progress}"/></Grid></Border></DataTemplate></CollectionView.ItemTemplate></CollectionView>
  <VerticalStackLayout><Label Text="Bitrate de saída (32–320 kbps)" Style="{StaticResource FieldLabel}"/><Picker ItemsSource="{Binding Bitrates}" SelectedItem="{Binding SelectedCompressBitrate}"/></VerticalStackLayout>
  <HorizontalStackLayout HorizontalOptions="End" Spacing="8"><Button Text="Iniciar" ImageSource="action_play.png" Style="{StaticResource PrimaryButton}" Clicked="Start" IsEnabled="{Binding CanStartCompressions}"/><Button Text="Pausar" ImageSource="action_pause.png" Style="{StaticResource SecondaryButton}" Clicked="Stop" IsEnabled="{Binding IsBusy}"/><Button Text="Continuar" ImageSource="action_resume.png" Style="{StaticResource SecondaryButton}" Clicked="Start" IsEnabled="{Binding CanResume}"/></HorizontalStackLayout>
 </VerticalStackLayout></Border></VerticalStackLayout></ScrollView><Border Grid.Row="1" Style="{StaticResource Card}" Padding="14"><VerticalStackLayout><Grid ColumnDefinitions="Auto,*,Auto"><Label Text="Pasta de saída" Style="{StaticResource FieldLabel}" VerticalOptions="Center"/><Entry Grid.Column="1" Text="{Binding OutputDirectory}" Margin="10,0"/><Button Grid.Column="2" Text="Escolher" ImageSource="action_folder.png" Style="{StaticResource SecondaryButton}" Clicked="Output"/></Grid><ProgressBar Progress="{Binding Progress}"/><Label Text="{Binding Status}" FontSize="11" TextColor="{StaticResource TextSecondary}"/></VerticalStackLayout></Border></Grid>
</ContentPage>
